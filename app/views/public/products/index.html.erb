<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'public/genres/genre_search', genres: @genres %>
    </div>
    <div class="col-md-9">
      <h4>商品一覧(全<%= @products.count %>件)</h4>
      <div class="clearfix">
        <% @products.each do |product| %>
          <div class="pull-left">
            <%= link_to product_path(product) do %>
              <%= attachment_image_tag(product, :image, :fill, 150, 180, fallback: "no_image.png") %><br />
              <%= product.name %><br />
            <% end %>
            ¥<%= product.tax_on.to_s(:delimited) %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>