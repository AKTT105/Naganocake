<div class="container">
  <h4>会員の方はこちらからログイン</h4>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "form_horizontal" }) do |f| %>
      <div class="col-md-9 col-md-offset-3">
        <div class="form_group">
          <%= f.label :email, "メールアドレス", class: "control-label col-md-2" %>
          <div class="col-md-6">
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
          </div>
        </div>
      </div>

      <div class="col-md-9 col-md-offset-3">
        <div class="form_group">
          <%= f.label :password, "パスワード", class: "control-label col-md-2"%>
          <div class="col-md-6">
            <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
          </div>
        </div>
      </div>

      <%= render "customers/shared/links" %>

      <div class="actions">
        <%= f.submit "ログイン", class: "btn btn-primary" %>
      </div>
    <% end %>
  </form>
</div>


<h4>登録がお済みでない方</h4>
<%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行ってください。
